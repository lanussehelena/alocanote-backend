server:
  port: 8080

spring:
  application:
    name: api

  datasource:
    url: jdbc:h2:mem:alocanotedb
    driver-class-name: org.h2.Driver
    username: sa
    password: '123'

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console